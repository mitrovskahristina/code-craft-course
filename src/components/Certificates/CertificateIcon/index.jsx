import './index.css'

const CertificateIcon = ( {imageSource} ) => {
    return (
        <div className="certificate_icon">
            <img src={imageSource} />
        </div>
    )
}

export default CertificateIcon